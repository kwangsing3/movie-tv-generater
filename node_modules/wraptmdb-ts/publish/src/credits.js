"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Credits = void 0;
/*
 * The MIT License (MIT)
 *
 * Copyright (c) kwangsing3
 *
 * https://github.com/wrapTMDB/wrapTMDB-ts
 *
 */
const axios = require('axios');
const c_module = require("./common");
const baseURL = c_module.GetURL();
class Credits {
    /********************
     * 1.GET /credit/{credit_id}
     * @description Get a movie or TV credit details by id.
     * @param {number|string} credit_id
     * @returns JSON
     * @doc https://developers.themoviedb.org/3/credits/get-credit-details
     ********************/
    async GetDetails(credit_id) {
        const token = c_module.GetToken();
        const header = c_module.GetHeader();
        const targetURL = baseURL + c_module.Route.CREDIT + `${credit_id}` + `?api_key=${token}`;
        if (token === 'UnitTest_api_key') {
            return targetURL;
        }
        const data = await axios.get(targetURL, header);
        return data.data;
    }
}
exports.Credits = Credits;
/*
1. GET Get Details
*/
//# sourceMappingURL=credits.js.map